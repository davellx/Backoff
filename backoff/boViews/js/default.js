// javascript included in every page (see the meta.php)
// it's really convenient, oh yes it is !

$(document).ready(function(){
    
    
});


function capitaliseFirstLetter(string)
{
    return string.charAt(0).toUpperCase() + string.slice(1);
}